So I wanna start making a to do markdown file. That's gonna include some things I need to fix with my site. We're gonna progressively add to this. And we are Let me think. One of the first things I wanna do is fix the social icons in the footer of pages. And And then unify the another item is unify the mega menu nav So to reference sort of a template type format to keep safe and be the source source of truth.
Apr 2
Created a file, read a file

Got it started! I kept it simple with two categories — Fixes for bugs like the footer icons, and Improvements for structural work like the mega menu. Just let me know the next item whenever you're ready and we'll keep building it out.
Site todos
Document · MD 

Let's add another to do item that includes finalizing the layout for albums in the photography section. Where it has the a 9 by 9 grid tile splashes, is the element name. That then goes down to a scrollable pane that has the White keys each indicating, an album that contains its own gallery of at least 9 by 9 photos in the group.
Apr 2
Edited a file, read a file

Added! I gave the photography section its own category since it's likely to grow. The structure — tile splash → scrollable white key album cards → individual 9×9 galleries — is captured as sub-bullets so the layout logic is clear at a glance. Keep 'em coming!
Site todos
Document · MD 

I need to make the viewer that from the splash tile or tile splash actually, Is the name be able to support In album like feature where or sets rather, I think I was calling it. The sets would include a cover, which would show on the tile, but not be included perhaps in the scroll. But the it needs to be able to support scrolling, to then like, show multiples of the different, like, almost like a gallery,
Apr 2
Edited a file, read a file

Updated! So the way it's captured now: Sets live inside the Tile Splash, each with a cover tile that's display-only, and when you open a Set the viewer scrolls through the photos gallery-style — cover excluded. Let me know if the terminology or structure needs any tweaking!
Site todos
Document · MD 

So, uh, we're gonna add a few things to the to do list. Um, one is, um, we need to have the album... or, actually
Apr 4

We're gonna add a section that creates, uh, sort of template for the meta tags of a page slash post that's gonna, um, work on a social preview. So in other words, it will have an SEO, like, light, um, description and information, um, but be very succinct, simple, and clean, um, to kind of work with Posting it through Discord, Twitter, um, and what I wanna do is provide a few, uh, background... sort of abstract backgrounds for the back... background of, like, the social post that, uh, the agent could just lay over some text, which I will try to help to give samples of, um, to create the image preview. And
Apr 4

Okay, so you're building a social preview system

Yeah. Okay. So
Apr 4

Got it — you want a meta tag template system that generates clean social previews for Discord, Twitter, and the like. And you're thinking abstract background images that an agent can layer text over to create those preview graphics. That's a smart setup. Should I add this as a new section to the to do

Got it. You want a meta tag?
Apr 4

Yeah, exactly. A template for meta tags that'll work with social previews across Discord, Twitter, and those platforms. With some abstract background options that an agent can layer text over to generate the preview image. Should I add that as a new section to the to-do

Yeah. Exactly.
Apr 4

Okay, so let me add that. You're creating

Okay. So let me add that.
Apr 4

Go for
Searched the web

Go for it!
Apr 4
Edited a file

Ah, looks like I don't have the file edit tool handy. Can you open that to-do file in your editor and I'll walk you through what to add? The social preview section should include the meta tag template, those abstract background templates you mentioned, and the image dimensions that work best across

Ah, looks like I don't have the file
Apr 4

Yeah, looks like I lost access to that tool. You'll need to add it manually to your to do list, but here's what I'd capture: Social Preview System section with meta tag template for Open Graph and Twitter Cards, keeping descriptions under 60 characters, plus a set of abstract background templates you can layer text over at 1200 by 630 pixels for that cross-platform

Yeah, looks like I lost access to that tool.
Apr 4

You'll need to add it manually to the file, but here's what to add: a Social Preview

You'll need to add a bit man --
Apr 4

Go ahead and add that Social Preview System section whenever you get a chance. I can help you flesh out details once it's in

Go ahead, and add that social pre --
Apr 4

Go ahead and paste it in. I can read it back to you to make sure it looks right.

Go ahead and paste it in. I can read it back to you to make sure.
Apr 4

Sure! Here's what we'd add to your to do list:

Social Preview System section with create meta tag template for pages and posts, support Open Graph and Twitter Card meta tags, keep descriptions succinct and clean for social sharing, optimize for Discord Twitter and other platforms, provide abstract background templates for social preview images, generate 1200 by 630 pixel OG images with text overlay capability, and create sample text variations for different content

Sure, here's what we'd add to your t -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- --
Apr 4

Social Preview System section under your Photography stuff. Here's the text:

Social preview system.
Apr 4

Right, so under Social Preview System, add:

Create meta tag template for pages and posts. Support Open Graph and Twitter Card meta tags. Keep descriptions succinct and clean for social sharing. Optimize for Discord, Twitter, and other platforms. Provide abstract background templates for social preview images. Generate 1200 by 630 pixel OG images with text overlay capability. Create sample text variations for different content

Right, so under social pre --
Apr 4

Here's what to add:

Social Preview System

    Create meta tag template for pages and posts

Here's what to
Apr 4

Social Preview System, create meta tag template for pages and posts. Support Open Graph

Social preview
Apr 4

Create meta tag template for pages and posts. Support Open Graph and Twitter Card meta tags. Keep descriptions succinct and clean for social sharing. Optimize for Discord, Twitter, and other platforms. Provide abstract background templates for social preview images. Generate 1200 by 630 pixel OG images with text overlay capability. Create sample text variations for different content

create meta tag template for pet
Apr 4

For pages and posts. Support Open Graph and Twitter Card meta tags. Keep descriptions

For pages and posts.
Apr 4

Support Open Graph and Twitter Card meta tags. Keep descriptions succinct and clean

Support Open Graph
Apr 4

And Twitter Card meta tags. Keep descriptions succinct and clean for social sharing. Optimize for Discord, Twitter, and other platforms. Provide abstract background templates for social preview images. Generate twelve hundred by six hundred thirty pixel OG images with text overlay capability. Create sample text variations for different content

and Twitter Card MetaTap.
Apr 4

Keep descriptions succinct and clean for social

Neat description
Apr 4

And clean for social

And clean for social.
Apr 4

Sharing. Optimize for Discord, Twitter, and other platforms. Provide abstract background

Sharing!
Apr 4

Twitter, and other

Twitter and other plat.
Apr 4

Platforms. Provide abstract background templates for social preview images. Generate 1200 by 630 pixel OG images with text overlay capability. And create sample text variations for different content

Platforms.
Apr 4

Abstract background templates for social preview

Abstract background temp.
Apr 4

Social preview images.

Social preview image.
Apr 4

Generate 1200 by 630 pixel OG images with text overlay

Generate twelve hundred by --
Apr 4

630 pixel OG images with text overlay

Six hundred and thirty pixel.
Apr 4

OG images with text overlay

Not sure why you're having discussion with seems like yourself and cutting yourself off. We just need to add to the to do list, a way a repeatable way to create social previews for pages. It doesn't have to be much. It doesn't have to be much. The moment. But We I would like to know what the agent would need access to in future steps for being able to post as an account on Discord and Twitter. I have an account for Twitter for this, and what that whole pipeline would entail with me providing a few, it's generating a few background images and then laying over just some Clean, what aboutism, Type To describe what it is that we're posting, whether it's a page, a new project, or just some interesting files. Right? It could just be some very Abstract, neat looking word. And we'll kinda, like, have a a conversation conversational pattern to kinda either suss that out for me or understand from a light larger, higher scale what it is The Post is about.
Apr 4

Got it — much cleaner framing. Let me just add that to the file now.
Edited a file, viewed a file, created a file

Added! The Social Preview System section covers three things: the repeatable pipeline with the conversational agent flow, what credentials and API access you'll need for Twitter/X and Discord when you're ready to wire that up, and a note to get those abstract background images sorted. What's next?
Site todos
Document · MD 

Okay. I didn't get a chance to look at that yet, but, I will. But what we need to take into consideration with is 1, how to make the social preview. Right? But then when the agent is making or helping make the post the announcement fee. Merge on to I 70 East. Need to, like, catalog each post because I'm gonna create like a timeline of posts and sort of begin to add this to, like, a a site map, but we need the agent needs to know or have a way in the claud.md what posts have been made, when they've been made, just to sort of a memory of, what's been done that it may not always need contextual or it might need or it might need contextual relevance to see what terms or posts have been made already.
Apr 4

Really solid thinking. So there are actually two layers here:

    The post itself — the social preview pipeline we already captured
    A post log / catalog — a running record the agent can reference so it knows what's been announced, when, and with what language, so it doesn't repeat itself or lose context between sessions

The catalog would live somewhere the agent can always read — probably a claude.md or a dedicated posts-log.md — and each entry would have things like the date, what was posted, which platforms, the headline or caption used, and maybe a tag for the type of post (new page, project, photo set, etc.).

That log then feeds into the site's timeline feature you mentioned, so it's doing double duty — agent memory and public-facing history.

Want me to add both of those items to the to-do list? I can capture the catalog structure too so when we build it out there's already a schema to work from.

And more context would be, this would be added as we go, so updated in the Claude MD file sort of as the memory bank of the agent. You Claude MD now, but this to do and relevance and in included there. Like I said, 4 both the agent file as well as post that I'll be making, or sharing links in this net. So
Apr 4

Got it — so the claude.md becomes the living brain of the whole operation. It holds:

    The to-do list (or a reference to it)
    The post log / catalog as entries get made
    Any context the agent needs to stay consistent across sessions — terminology, naming conventions, what's been built, what's been announced

So it's not just memory for posting — it's the source of truth for the whole site project. The agent reads it at the start of any session to get its bearings.

Want me to add a to-do item to scaffold out the claude.md structure itself? That way we have a plan for what goes in it before we start filling it — things like sections for the post log, site map progress, naming conventions, and a pointer to this to-do file.
Ran a command, read a file

Added. The claude.md section is now in there with the post log structure baked in. When you're ready we can actually start drafting that file — it'll be the first thing any future agent session picks up to know where the project stands. What else you got?
Site todos
Document · MD 

Even further context, I have a Claude MD5 file that I don't have access to give you at the moment. But, what you sort of written up, which I haven't looked at, should easily be slotted or folded into that. I'll provide the other for and contrast, to kind of include inside of a larger scoped, cloud file. But this will be more than just the cloud file. It'll be for any agent. Right? We're just kinda using the cloud one at the moment, to kinda get at the boilerplate for other agents, and we'll work through their idiosyncrasies, a later date.
Apr 4

That makes total sense. So what we're really building is an agent-agnostic project file — the claude.md just happens to be the starting point because that's what we're working with now. But the structure, the post log, the terminology, the site map — all of that should be portable and clean enough to hand to any agent down the road, whether that's a different Claude project, a GPT, a local model, whatever.

When you do get access to your existing claude.md, we can do a merge pass — take what's already in there, fold in what we've drafted here, and shape it into something that works as the master agent brief. The goal being one well-structured file that any agent can read cold and immediately understand the project, the conventions, and where things stand.

I'll make a note of that in the to-do so we don't lose it.
Ran a command, read a file

Updated. The framing is now agent-agnostic — claude.md is just the current vehicle, not the destination. When you surface that existing file we'll do a proper merge and get it shaped into something clean enough to hand off to anything. What else is on your mind?
Site todos
Document · MD 

And just to wrap up, please add, like, what we'll be adding to the cloud file to the to do because we're gonna have to maintain the cloud file is what I'm getting at. So the to do is mainly the first parter before this was the site preview, but then also, connecting that to the agent file that we'll be folding our changes into. Hopefully, that adds, clarity.
Apr 4

Totally clear. The to-do isn't just site fixes — it has a second job, which is tracking what needs to be folded into the agent file as we go. So maintaining the agent file becomes its own ongoing item, not a one-time task.
Ran a command, read a file

That's the full picture now. The to-do covers site work, the social preview pipeline, and maintaining the agent file as an ongoing responsibility — not just a setup step. When you're ready to share that existing claude.md we can do the merge and get everything consolidated.
Site todos
Document · MD 

Another cleanup item is the mobile nav where it has it changes from the mega menu to the vertical list type style, there is some hovering colors. So on the white background or light mode, The blue text for the links goes white. Make it unreadable, on hover. Currently. So that's 1. And 2 would be animating the mobile drop down, which is kind of a bunch of triangles, to simulate hamburger menu bar. Almost like hamburger bar, but, more stylized. And then 3 would be there is a weird animation on an SVG in the footer that is the back to top link that just has the chevron pointing up In certain weird edge cases, scenarios, it's, cycling through colors. I believe I know what the issue is. That needs addressed.
Apr 4
Ran 2 commands, read a file

Three new items added to Fixes — the light mode hover readability bug, the hamburger triangle animation, and the footer chevron color-cycling glitch. What else?
Site todos
Document · MD 

There are a few of the I forget if I use the font awesome icons or the material icons for the contextual links down in the footer that, go out to 3rd party sites that the icons are not, displayed correctly for a few of them. Off the top of my head, I don't know, which exactly they are. There's a few that are working, which makes me think that there's a misaligned font. Awesome. Library being sourced.
Apr 4

Good diagnosis — if some icons render and some don't, it's almost always a version mismatch or the wrong CDN link pulling in a partial set. Classic Font Awesome gotcha where FA5 and FA6 use different class names (fas/far vs just fa-solid/fa-regular), so mixing them breaks selectively.
Ran a command, read a file

Added. When you go to fix it, the quickest way to confirm is just checking what version is being sourced in your <head> — if it's FA6 but any icon is still using fas or far prefixes from FA5, those will silently fail. What else?
Site todos
Document · MD 

I'd also like to make I'm not sure if a gulp file would be the best, Apple the my JavaScript and CSS So adding some sort of webpack task runner that could be triggered to kind of to minify the CSS and JavaScript. It's currently mostly a static site, so we're not, necessarily building the app. But we kind of are. I'd like to hear feedback on, like, what a more a lightweight solution, if it's just a gulp file or something akin to that. That gets processed as its own again, I'm a little confused on the CloudOps engineering side of how I wanna do this. Because while I'm developing, it's nice to have the file separate
Apr 4

Good instinct. For a mostly-static site you don't need the full webpack overhead — that's really built for bundling JS modules in an app context. For what you're describing, a few lightweight options:

Gulp — still solid for this use case. You'd have a watch task during dev that keeps files separate and readable, then a build task that minifies and concatenates for production. Low config overhead.

esbuild — extremely fast, handles both JS and CSS minification, very minimal config. Good if you want something modern without the Gulp ceremony.

Vite — worth considering even for static sites. Dev mode serves files individually with hot reload, build mode outputs minified bundles. Becoming the go-to for this middle ground between "just static" and "full app."

Just npm scripts — honestly for a simpler site you can wire up terser for JS and clean-css directly in package.json scripts without a task runner at all.

My lean for your situation would be Vite or esbuild — Gulp is a little dated and the ecosystem has moved. But the key thing either way is the same pattern you described: separate files in dev, minified output for prod, triggered as a build step.

Want me to add this to the to-do as an open question to resolve, with these options noted so you have the context when you come back to it?

So based on those, answers, it seems like the ES build Gulp, or just NPM scripts are the best 3. I do like the automation that it would generate the minified files local when there are changes, and I would only commit changes that are correct, that workflow is pretty familiar to me. I believe, though, we I would have to have Gulp running, while I'm working to be generating the minified files and watching for changes, I almost want a task runner script that could manually force it to regenerate, one in case I don't know. I just wanna do a version change and there isn't much changes or any in the CSS or
Apr 4

Yeah that workflow makes total sense — Gulp watching in the background during active dev, but also a manual trigger you can run anytime to force a clean regenerate without needing a file change to kick it off. Something like gulp build or an npm script alias like npm run minify that just does a one-shot pass regardless of what changed.

That actually points toward Gulp + npm scripts together being the right call — Gulp handles the watch/auto workflow during dev, and you expose a few named npm scripts in package.json as the manual triggers so you don't have to remember Gulp task names. Clean separation, familiar, and easy to hand off to another agent or developer later.
Ran a command, read a file

Added as its own section. The key detail is captured — watch mode for dev, manual trigger for forced regeneration, and source files staying separate from minified output so you control what gets committed. What else?
Site todos
Document · MD 

Is there a way to include the golf or the almost manual trigger in the GitHub action? Could this be done, once changes get pushed to the GitHub pages side of things. I'm not sure if this grest or need
Apr 4

Yes, absolutely — this is actually a very common and clean pattern. A GitHub Action can run your Gulp build (or npm script) automatically on every push, so the minified files get regenerated in CI rather than you having to remember to do it locally before committing.

The basic flow would be:

On push to main → GitHub Action runs npm run build → commits the minified output back to the repo (or deploys directly to GitHub Pages)

A couple of ways to handle it:

    Build in CI, commit back — Action runs the minify task and pushes the generated files back to the branch. Keeps minified files in the repo as a source of truth.
    Build in CI, deploy directly — Action runs the build and deploys straight to GitHub Pages without committing minified files back. Cleaner repo, but minified files only exist in the deployment.
    Hybrid — You still have the local manual trigger for development/verification, but the Action is the canonical build step on push. Best of both worlds.

Option 3 sounds like it fits your workflow best — you're already comfortable committing verified changes, and the Action just becomes a safety net and the official production build step.

Want me to add this to the to-do under the build pipeline section?

Yeah. And I can see use cases for both actually where if I'm working on a file, it generates and we push the commit to master and it gets act like, just added. But I do intend to have a type of agent working on files and what it pushes would get generated on the GitHub side. So a user build and an agent build might be what we think about doing. Thoughts?
Apr 4

Really smart distinction. The two build paths have genuinely different needs:

User build — you're in dev, Gulp is watching, you're verifying the minified output locally before committing. You want to see what's going in. The GitHub Action on push is more of a safety net or final stamp.

Agent build — the agent is modifying files programmatically and pushing commits. It probably shouldn't be expected to run Gulp locally, so the GitHub Action becomes the primary build step. The agent pushes source changes, GitHub handles the minification and either commits back or deploys.

This also has a nice safety implication — the agent never directly commits minified files, only source. The Action is the gatekeeper that produces the production output. Keeps agent commits clean and auditable.

You could differentiate them with either separate workflow files (e.g. user-build.yml vs agent-build.yml) or just branch conventions — like agent commits always go to a specific branch that triggers its own Action before merging to main.
Ran a command, read a file

Captured. The key architectural decision is in there — agent never touches minified files, Action is its build authority, and the branch/workflow separation keeps the two paths from stepping on each other. That staging branch idea also gives you a natural review point before agent changes hit main. What else?
Site todos
Document · MD 

My only question now is how would GitHub be told to distinguish between receiving just a push to master and running the user build for noticing changes pushed and running the agent build and how do we flip flop or use distinguish between which one is gets used?
Apr 4

Good question. GitHub Actions has a few clean ways to handle this:

Option 1 — Branch convention (simplest)

    main → triggers user build workflow
    agent or agent/feature-name → triggers agent build workflow
    Each workflow file has a branches: filter so they only fire on their respective branch. Agent changes get merged to main via PR after the agent build passes.

Option 2 — Commit message tag (flexible)

    Any push to main, but the Action checks the commit message for a tag like [agent]
    If tag is present → agent build. If not → user build.
    Simpler branch structure but requires the agent to always include that tag, which is easy to instruct in the agent file.

Option 3 — Who pushed (actor check)

    GitHub Actions exposes github.actor — the username that triggered the push
    If you have a dedicated bot account for the agent, the workflow can branch on that
    Clean but requires a separate GitHub account for the agent

My recommendation for your setup would be Option 1 + Option 2 as a fallback — branches are the cleanest signal, and the commit message tag gives you an escape hatch if you ever need to force an agent-style build from your own account.

Want me to add this decision to the to-do with these options noted so it's there when you go to wire up the Actions?

Let's go with your recommendation, and make 2 paths and we'll do e branch that the the or create a branch from master and push and merge back in.
Apr 4

Perfect — clean and straightforward. So the flow would be:

    main — your branch, user build fires on push
    agent — agent's working branch, agent build fires on push, then merges back to main via PR when done

And the commit message tag as a fallback for edge cases where you need to manually trigger an agent-style build from your own account without switching branches.
Ran a command, read a file

Locked in. Two workflows, two branches, one merge path back to main. When you're ready to build these out the workflow files themselves are pretty short — maybe 20-30 lines each. Easy to scaffold when the time comes. What else?
Site todos
Document · MD 

Let's do some research on how the agent will actually be coating I kind of anticipate there being, like, 2 paths. 1 where it's being ran sort of locally, and the other would be through cloud integration where it would something I would do would fire it to kinda like, if a new to do is added somewhere, and it gets assigned, let's say, in issue. If the agent gets assigned an issue, it could then, work on it with the context.
Apr 4

Great — let me research the current landscape for both paths.
Searched the web

This is really well-timed research — the tooling here is actually purpose-built for exactly what you're describing. Here's the lay of the land:

Path 1 — Local (Claude Code CLI) Claude Code is an agentic coding tool that reads your codebase, edits files, runs commands, and integrates with your development tools — available in your terminal, IDE, desktop app, and browser.
Claude
You'd run it locally with a watch or on-demand, it has full access to your files, and it's the hands-on dev workflow you'd use while actively building.

Path 2 — Cloud via GitHub Issues (the one you described) This is already a real, supported thing. When a ticket comes in for a minor fix, a small refactor, a small feature — you simply mention @claude on the issue with a clear description. Claude picks it up, creates a branch, makes the fix, runs the tests, and opens a PR.
Medium

And for the assignment trigger specifically — when a user reports a bug and it gets logged as an issue in GitHub, a developer can assign it to Claude by commenting something like @claude fix the TypeError in the user dashboard component.
Eesel AI

The key piece that ties it all together: Claude respects your CLAUDE.md guidelines and existing code patterns
Claude
— so that agent file we're building becomes the context it reads before touching anything.

Want me to add both paths to the to-do with setup notes?

Right. But the CLI tool would need to be running, to then trigger it What I'm trying to figure out also is how what I would need I would need to set up the profiles to give access to the agent to work on issues. So in other words, how do I make a sort of, account the agent to both see, issue, and work on changes through branching.
Apr 4

Good question — and the answer is actually cleaner than you might expect. You don't need the CLI running locally for the cloud path at all. Here's how the two are separated:

Local path (CLI) Claude Code runs on your machine. You're present, it watches files or you trigger it manually. No special GitHub account needed beyond your own.

Cloud path (GitHub Issues → agent) This runs entirely on GitHub's infrastructure via GitHub Actions. The agent needs:

    An Anthropic API key — stored as a GitHub repository secret (ANTHROPIC_API_KEY). This is what gives the Action access to Claude.
    The Claude GitHub App installed on your repo — you install the Claude GitHub app to your repository by visiting https://github.com/apps/claude and add ANTHROPIC_API_KEY to your repository secrets.
    Steve Kinney
    This is what lets it see issues, read the repo, and post comments.
    A workflow .yml file — this is the instruction file that defines what triggers the agent and what permissions it has. The workflow grants permissions for contents: write, pull-requests: write, and issues: write
    Groundy
    — those three cover everything the agent needs: reading issues, creating branches, pushing code, and opening PRs.

So there's no separate "agent account" per se — it runs as claude[bot] under the GitHub App identity. You own the API key, you control the permissions in the workflow file, and the CLAUDE.md is what tells it how to behave on your specific project.

Want me to add the setup checklist for this to the to-do?

Okay. Great. Add your findings now as the a to do in a research phase of adding agents to the project workflow. Include your detail and step by step, with it.
Apr 4
Ran a command, read a file

All in there. The section is broken into the two paths with step-by-step setup for each, how the agent identity works, and a note tying it back to the CLAUDE.md and the two-branch build pipeline we already defined. When you're ready to actually set it up, this should be enough to walk through start to finish without needing to look much else up. What's next?
Site todos
Document · MD 

Right. Turn left. I hope you included sort of a redundant inclusion there. Morris Road. That both details to the user but also a version that would be included into the agent markdown so that If there was an issue with one of the accounts, it could diagnose and say it was missing access, and something of the like. So it gives context to both the user to set it up, but also the agent to Check to see if there are any issues with the account API keys, tokens, or the like.
Apr 4

Good call — that's a really useful dual-purpose layer. The setup notes as they are now are written for you as the human. We need a second version of that same information written for the agent so it can self-diagnose if something is misconfigured or missing.
Ran a command, read a file

Added. The self-diagnostic block is written in the agent's voice — it's ready to be copy-pasted directly into CLAUDE.md as-is. It covers API key validation, GitHub App access, branch permissions, and a general fallback rule that says never silently proceed if something is wrong. That last one is important — you don't want the agent guessing around a missing token and making a mess. What else?
Site todos
Document · MD 

Okay. So we're gonna add something to the to do but it's gonna be one thing that turns into a few things. Gonna start including Anywhere that an agent is being referred to or has a a touch point of interaction, We'll mention agent. And in even setting up the accounts or pushing of code or, approving of a PR, we need to have users slash me, Joel, my touch points where I would need to Do something to I would be a blocker to the agent actually releasing code to live. And anywhere that I would need to me personally log in or create an account or, start something that then gives access to the agent. So create a to do that works through this because this is gonna be an ongoing thing, but We need to start going through and Adding Joel versus agent touch points on code or process.
Apr 4

Perfect — this is essentially a responsibility matrix. Anywhere in the workflow there's a handoff, a gate, or an approval, we need to know if it's Joel or the agent. And critically, Joel's touch points are the blockers — nothing goes live without them.
Ran a command, read a file

That's now a living matrix. Every workflow area — accounts, dev, build, deployment, social posting — has ownership tagged. Two open questions are flagged as TODOs where you'll need to make a call: the exact deployment trigger, and whether social posts need a manual confirm or just a review window. Those are yours to decide when you get there. What else?
Site todos
Document · MD 

Let's add a research item that essentially is how does the agent Ask questions, notify me, assuming it would be a great way it would be great to use Discord for this that it would just ping me either in my server or DM of some kind to ping with questions, Because I wanna create something like a flagging system that continues off of what we just had that if it runs into any high token Ask or task or if it has any questions for clarity, It it tries to run as many things as it can. And then it'll either ask alacarte like 1 by 1 or bundle them to save token usage to have like a list of questions that need answered. And figuring out a way to get the answers back to it. So, like, even if, let's say, it's able to ping me with questions in Discord, Discord. Will it then be able to read The the answers if that's the medium Discord or a GitHub comment. I think it would probably be It would be a Discord message of a GitHub issue where I would then reply with the contextual answer, I think, is what I just kinda figured would be great. Thoughts?
Apr 4

Really solid thinking — and you've actually landed on the cleanest pattern yourself at the end there. Let me share my take:

Discord for pings, GitHub for answers is the right split. Here's why:

Discord is great for pushing notifications to you — low friction, you're already there, it gets your attention. But it's a bad place for the agent to read back answers because Discord's threading is loose and the agent would need a bot continuously polling for replies. That's more infrastructure to maintain.

GitHub issues are already the agent's native environment. If the agent opens a comment on the issue it's working on with a bundled list of questions, you reply in that thread, and the agent reads it on its next run — that's clean, auditable, and requires zero extra setup beyond what you're already building.

So the flow would be:

    Agent hits a blocker or question → bundles them (to save tokens) → posts as a comment on the relevant GitHub issue
    Agent pings you on Discord via webhook: "I have 3 questions on issue #12 before I can continue"
    You go to GitHub, answer in the thread
    Agent resumes, reads your answers from the issue comment, continues

The Discord webhook is actually the easy part — it's just an outbound HTTP call, no bot account needed for posting. Reading answers back from Discord would require a bot and polling, which is why GitHub is cleaner for the reply side.

Want me to add this as a research item with that proposed flow documented?

Okay. Let's make sure that's added to the to do's and create like a section in the to dos that we're gonna port that to do list item into the human's text. Humans markdown, and that's also going to be reiterated and echoed back into the Claude Mark down so that it knows how to send messages and alerts and human's message humans mark down to No. Where and how to reply to said alerts and messages.
Apr 4

Got it — so this becomes a three-way sync:

    To-do captures the work item
    humans.md — a new file, written for you (Joel), explaining where and how the agent will reach you and where/how to reply
    CLAUDE.md — the agent's version, telling it how to send alerts, bundle questions, and where to look for answers

All three stay in sync so neither party is confused about the communication protocol.
Ran a command, read a file

All in. A few things worth noting in what was added:

The humans.md file is now a to-do item in its own right — that's a new file we'll need to create, written entirely for you, not the agent. Think of it as your half of the communication contract.

The bundling logic has a carve-out — if something is fully blocking all progress, the agent pings immediately rather than waiting to batch. Otherwise it holds questions and sends them together.

And the private Discord channel over a DM is recommended so you end up with a searchable log of every alert the agent has ever sent. Useful when you come back to something weeks later and want to know why a decision was made.

Ready to keep going or want to review what we have so far?
Site todos
Document · MD 

Yeah. I already have a human's text, and it its purpose is essentially Lightweight, streamlined, documentation living inside of the project And is constantly updated and referred back to with any process but also is a version slash change log and the agents will be much of the same, but just for different audience, obviously. The to do Well, is an ongoing thing that is gonna be is one file to, like, add things to that then get chained split to either humans or agents sort of workflow. Right? So that's why there's the 3 paths. And 3 files I have a human's file. I have a cloud file. What I really just need is the detail of everything we're talking about doing included in the to do so that it can get moved over into those other files once I get home. And we can start breaking those to dos out into the other constituent files.
Apr 4

Perfect — that clarifies the whole system cleanly. The to-do is the inbox. Everything lands here first, gets fleshed out with enough detail, and then gets sorted into humans.md or CLAUDE.md when you're back at your desk. No need to duplicate or pre-sort right now — just make sure the to-do is detailed enough that the migration is straightforward later.

So the three files are:

    todo.md — the inbox, where everything lives until it gets moved
    humans.md — Joel's reference, process docs, version/change log, written for a human
    CLAUDE.md — agent's briefing, same content different audience, instructions and context for any agent

Everything we've added today is already detailed enough to split out. When you get home the job is just reading through the to-do and asking "does this belong in humans, claude, or both?" and moving it over.

Want me to add a small legend or header note at the top of the to-do file that captures this three-file system so it's clear to anyone — including a future agent — what this file's role is and where things go from here?

Alongside the agent workflow and human workflow, I also might want to have the agent a a task. We'll be using t shirt sizes for the complexity slash effort, and how we'll kinda determine those sizing is And how a mile, use the right lane to take the ice A small would be to Washington. It may be touching many files, but it's relatively each to understand what was affected. A medium would be 2 or, 2 or less languages and 2 to 3 or more files, are changed or affected. And then a large would be next to full stack, HTML, JavaScript, CSS, a platform API, or anything of the like. So let's include how to determine, rate and judge, like, the tasks for the
Apr 4

Good system — t-shirt sizing keeps it simple and gives the agent a shared language with you for scoping work before it dives in. Let me capture the sizing definitions and add the task rating framework to the to-do.
Ran a command, read a file

Added. I also slipped in an XL tier as an implicit escalation — anything that crosses into architectural or infrastructure territory should never just get started, it needs a written plan from the agent first that you sign off on. That ties back into the flagging and blocker system we already defined.

When you do the migration pass into humans and claude files, one of the quick wins will be tagging all the existing to-do items with their size — S through XL — so the agent already has a prioritized, scoped backlog to work from on day one. What else?
Site todos
Document · MD 

That's actually really great feedback. For the XL. It would need further or a line of questioning to cover any unknowns or possible oversights. Great. Yeah. Definitely, include that and yeah. Like I said, have this in the to do and then we're gonna add this to the human agents file, because I want, a human agent and Claude. Claude would be, like, specific for, you know, Claude's gonna be the boilerplate for the rest of the agents files, but, Claud may not be the only thing that we'll be using and the agent's file is to kinda give a broader context of the project and just less back and forth and setup and preliminary understanding for it to get up and running. Whereas Claude or the the lead agent would, be more purpose. Driven.
Apr 4

Really clean architecture. So the file hierarchy is actually four files not three:

    todo.md — the inbox, everything lands here first
    humans.md — Joel's reference, process docs, change log, written for a human
    agents.md — broad project context, boilerplate briefing for any agent regardless of platform, minimal setup friction, gets any new agent up to speed fast
    CLAUDE.md — Claude-specific, purpose-driven, inherits from agents.md but adds Claude-specific instructions, lead agent behavior, communication protocols, the works

So agents.md is the onboarding doc and CLAUDE.md is the operating manual. Any future agent — GPT, Gemini, local model — reads agents.md and can function. Claude reads both and operates at full capacity.
Ran a command, read a file

That's the full picture now. The XL two-step is in there — questions first, then plan, then approval, then work. And the migration checklist at the bottom maps every section we've built today to which file it belongs in, so when you sit down tonight the sorting pass is already planned out. What else?
Site todos
Document · MD 

one last thing. the file for site to-dos is just TODO.md that way it can be found and distinguished for other HUMANSand AGENTS to look through and update what it/they needs to
10:26 PM

Good call — simple, uppercase, conventional. Any agent or human coming to the repo cold will find it immediately.
Ran 2 commands, read a file

Renamed and the header is in place. TODO.md — easy to spot, impossible to miss, clear about its role. Drop it in the repo root alongside HUMANS.md and CLAUDE.md and the whole system is self-explanatory to anyone who opens the project.
Todo
Document · MD 

um forgot can you consolidate the files. this was a prev version from a dif instance i believe (past)
10:37 PM

Let me read both files first before merging.
Reading the uploaded previous TODO.md

Good news — the uploaded file and what we built this session are actually nearly identical. The uploaded one is a slightly earlier snapshot that's missing the header and the image/banner at the top. The content is the same, just the previous version doesn't have the TODO.md header block we added at the end of our session.

The only unique thing in the uploaded file is the banner HTML at the very top — lines 1–3 with your site image link. Let me merge that in and produce the clean consolidated file.
Ran a command, read a file

Clean merge. The only difference between the two versions was your site banner at the top — that's now included. Everything else was already in sync since the uploaded file was just an earlier snapshot of the same session's work. Drop this into your repo root and you're good to go.
Todo
Document · MD 
Want to be notified when Claude responds?


Claude is AI and can make mistakes. Please double-check responses.
Todo · MD
<a href="http://joeldom.github.io/redesign/sandbox/" target="_blank">
  <img class="aligncenter" alt="Joel Dombek Design 2022" src="https://raw.githubusercontent.com/joeldom/asset/main/twitter-header.png" width="847" height="120" />
</a>
 
# TODO.md
 
> The project inbox. Everything lands here first.
> Maintained by Joel and any assigned agent.
> Items get migrated into `HUMANS.md`, `AGENTS.md`, or `CLAUDE.md` during review sessions.
> When in doubt, add it here.
 
---
 
# Site To-Do List
 
## 🐛 Fixes
 
- [ ] Fix social icons in the footer of pages
- [ ] **Footer third-party link icons not rendering**: Some Font Awesome (or Material) icons for external/social links in the footer are broken — likely a version mismatch (FA5 vs FA6 class names differ) or wrong CDN source pulling a partial icon set. Audit which icons are broken and confirm a single consistent library version is sourced
- [ ] **Mobile nav — hover color bug**: In light mode, blue link text turns white on hover, making it unreadable against the white background
- [ ] **Mobile nav — hamburger animation**: Animate the stylized triangles that form the hamburger-style menu toggle (similar to a hamburger bar but more stylized)
- [ ] **Footer SVG — back to top chevron**: Rogue color-cycling animation on the upward chevron SVG in certain edge case scenarios — likely a known issue with the animation state
 
 
## ⚙️ Build & Asset Pipeline
 
- [ ] Set up **Gulp + npm scripts** for CSS and JS minification
  - Gulp watch task runs during dev — auto-minifies on file change, keeps source files separate
  - Manual force-regenerate available via npm script (e.g. `npm run minify` or `npm run build`) for cases like version bumps with no file changes
  - Only commit minified output when it's confirmed correct — source and minified files tracked separately
  - Tasks to cover: JS minification, CSS minification, optional concatenation
  - Keep config lightweight — this is a static site, not a full app bundle
 
- [ ] Define **two build paths** — user build and agent build
  - **User build**: Gulp watch runs locally during dev, minified output verified before commit, GitHub Action on push acts as a safety net / final stamp
  - **Agent build**: Agent only commits source file changes — GitHub Action is the primary build step, handles minification and deployment. Agent never commits minified files directly, keeping its commits clean and auditable
  - Consider separate workflow files (`user-build.yml` / `agent-build.yml`) or branch conventions to differentiate the two paths
  - Agent commits could target a staging branch that runs its own Action before merging to main
 
- [ ] Set up **two GitHub Actions workflow files**
  - `user-build.yml` — triggers on push to `main`, runs Gulp/npm minify as a safety net stamp
  - `agent-build.yml` — triggers on push to `agent` branch, runs full minify build, then merges back to `main` via PR
  - Fallback: if a commit to `main` includes `[agent]` in the message, `user-build.yml` defers to agent build logic
  - `agent` branch is created from `main`, agent works there, PR back to `main` on completion
 
## 🔧 Improvements
 
- [ ] Unify the mega menu nav to reference a template/component as the single source of truth
 
## 📣 Social Preview System
 
- [ ] Create a repeatable social preview pipeline for pages, projects, and posts
  - Agent converses to understand the post at a high level (new page, project, file drop, etc.)
  - Generates a clean, abstract short-form description or headline to overlay on the image
  - Uses a set of pre-made abstract background images as templates
  - Lays text over background to produce the preview graphic (1200×630px)
 
- [ ] Document what the agent will need for auto-posting
  - **Twitter/X**: Developer app with OAuth 2.0, site account credentials, media upload + post endpoints
  - **Discord**: Bot token or webhook URL for the target server/channel, rich embed support
  - Both platforms need: generated preview image, short caption, and a URL
 
- [ ] Source / generate a small set of abstract background images for the preview templates
 
## 📸 Photography Section
 
- [ ] Finalize the layout for the Photography albums page
  - Top section: **Tile Splash** — 9×9 grid element
  - Below: scrollable pane with album cards ("white keys"), each representing an album
  - Each album contains its own gallery of at least 9×9 photos
 
- [ ] Build out the **Tile Splash viewer** to support **Sets**
  - Each Set has a **cover image** — displayed on the tile in the Tile Splash
  - Cover is not included in the Set's scroll/gallery view
  - Viewer supports scrolling through multiple photos within a Set (gallery-style)
  - Tile Splash should be able to display multiple Sets
 
## 🧠 Agent Memory & claude.md
 
- [ ] Scaffold a universal agent project file (currently `claude.md`) — portable across Claude, GPT, local models, or any future agent
  - When existing `claude.md` is available, merge and reconcile both into one master brief
  - Any agent should be able to read it cold and immediately understand the project
  - Reference to this to-do list
  - Site terminology and naming conventions (Tile Splash, Sets, White Keys, etc.)
  - Site map progress — pages built, pages planned
  - Post log / catalog (see below)
 
- [ ] Build out a **Post Log** inside `claude.md` — updated after every post/announcement
  - Fields per entry: date, platform(s), post type (page / project / photo set / other), caption or headline used, URL shared
  - Gives the agent memory of what's been said, when, and how — avoiding repetition
  - Also feeds the site's public-facing timeline feature
 
## 🔄 Ongoing — Agent File Maintenance
 
- [ ] Merge this to-do and site context into the existing `claude.md` once available
- [ ] Keep the agent file updated as a living document — not a one-time setup
  - Add new to-do items here AND reflect them in the agent file
  - Log completed work into the agent file so future sessions know what's done
  - Update post log after every announcement or social post
  - Update site map section as new pages/sections go live
  - Update naming conventions if terminology evolves (e.g. Sets, Tile Splash, White Keys)
- [ ] Ensure the agent file stays portable — clean enough for any agent to read cold
 
## 🤖 Agent Integration — Research & Setup
 
> Research phase: adding Claude Code as an active agent in the project workflow.
> Two distinct paths — local CLI for active dev, cloud via GitHub Actions for issue-driven work.
 
---
 
### Path 1 — Local (Claude Code CLI)
- [ ] Install Claude Code CLI globally
  - Requires Node.js 18+ (Node 22 LTS recommended)
  - Install: follow setup at https://claude.ai/code
  - Authenticate via OAuth flow linking your Anthropic account, or set `ANTHROPIC_API_KEY` env var
- [ ] Run `claude` in the project directory during active dev sessions
  - Has full access to local files, git, and terminal tools
  - Use `--dangerously-skip-permissions` / Auto mode for longer unattended runs
  - Background agents can run tasks while you stay focused: `& npm run dev` style
- [ ] Keep `CLAUDE.md` in the project root — CLI reads it on every session for context
 
---
 
### Path 2 — Cloud via GitHub Issues & Actions
> Agent runs on GitHub's infrastructure. No local CLI required. Triggered by issue assignment or `@claude` mentions.
 
#### One-time setup steps:
- [ ] **Install the Claude GitHub App** on your repository
  - Visit https://github.com/apps/claude and install to your repo
- [ ] **Add API key to GitHub Secrets**
  - Go to repo Settings → Secrets → Actions → New secret
  - Name: `ANTHROPIC_API_KEY`, value: your Anthropic API key
  - Never commit the key directly to the repo
- [ ] **Add the workflow file** — create `.github/workflows/claude.yml` in your repo
  - This defines what triggers the agent and what it's allowed to do
  - Minimum permissions needed:
    - `contents: write` — read files, create/push branches
    - `pull-requests: write` — open and update PRs
    - `issues: write` — read issues, post comments
  - Uses `anthropics/claude-code-action@v1` as the Action
 
#### How it works once set up:
- Assign an issue to Claude or comment `@claude fix X` on any issue or PR
- Agent reads the issue, checks out the repo, reads `CLAUDE.md` for your project conventions
- Creates a branch, makes the changes, runs any configured checks
- Opens a PR back to your repo for your review — you decide what merges
 
#### Agent identity:
- Runs as `claude[bot]` — no separate GitHub account needed
- You own the API key and control all permissions via the workflow file
- `CLAUDE.md` is the agent's briefing document — keep it current
 
---
 
### Notes on both paths:
- [ ] Decide which model tier to use per path
  - Local/interactive: Sonnet is the workhorse, Opus for complex reasoning tasks
  - GitHub Actions: defaults to Sonnet — switch to Opus in workflow config for harder tasks
- [ ] `CLAUDE.md` serves both paths — one file, keep it the single source of truth
- [ ] Agent branch (`agent`) + GitHub Action = cloud path's build pipeline (ties into the two build paths already defined above)
 
---
 
### 🔍 Agent Self-Diagnostic Block (for CLAUDE.md)
> The following should be folded into `CLAUDE.md` verbatim or adapted.
> It gives the agent the context to check its own access and surface clear errors.
 
---
 
**AGENT: On every session start, verify the following before taking any action:**
 
#### API & Authentication
- Confirm `ANTHROPIC_API_KEY` is present and valid — if missing or rejected, halt and report:
  > "ANTHROPIC_API_KEY is missing or invalid. No actions can be taken. The repository owner needs to add or rotate the key under Settings → Secrets → Actions."
- If running via GitHub Actions, confirm the workflow has the correct permissions block:
  - `contents: write` — required to read files and push branch changes
  - `pull-requests: write` — required to open and update PRs
  - `issues: write` — required to read issues and post comments
  - If any permission is missing, report which one and what it blocks
 
#### GitHub App Access
- Confirm the Claude GitHub App is installed on this repository
- If issues or PRs are not readable, report:
  > "Claude GitHub App may not be installed or may lack access to this repository. Visit https://github.com/apps/claude to check installation."
 
#### Branch Access
- Confirm the `agent` branch exists and is accessible
- If pushing to `agent` branch fails, report the error and do not attempt to push to `main` directly
- All agent work goes to the `agent` branch — never commit directly to `main`
 
#### CLAUDE.md / Project Context
- Confirm `CLAUDE.md` is present in the project root and was loaded
- If missing, report:
  > "CLAUDE.md not found. Proceeding without project context — results may not follow site conventions. Please restore CLAUDE.md to the project root."
 
#### General Fallback
- If any check above fails, do not silently proceed
- Surface a clear, specific error message describing what is missing and what the repository owner needs to do to fix it
- Do not guess or work around missing access — report and halt
 
---
 
## 👤 Joel vs 🤖 Agent — Touch Point Responsibility Matrix
 
> Ongoing. Every process, account, workflow step, or code interaction needs to be tagged.
> Joel's touch points are hard blockers — the agent cannot proceed past them without Joel's action.
> This section grows as new workflows are added. When in doubt, default to Joel as the blocker.
 
---
 
### Legend
- 👤 **Joel** — must be done by Joel personally (login, approval, account creation, merge, release)
- 🤖 **Agent** — agent handles autonomously
- 🔀 **Handoff** — agent prepares, Joel approves before anything moves forward
 
---
 
### Accounts & Access Setup *(one-time, but Joel-only)*
- 👤 Create and own the Anthropic account and API key
- 👤 Add `ANTHROPIC_API_KEY` to GitHub repository secrets
- 👤 Install the Claude GitHub App on the repository (https://github.com/apps/claude)
- 👤 Create and own the Twitter/X developer app and OAuth credentials
- 👤 Add Twitter/X API credentials to GitHub secrets for the social post pipeline
- 👤 Create and own the Discord bot token or webhook URL
- 👤 Add Discord credentials to GitHub secrets
- 👤 Create the `agent` branch from `main` initially
- 👤 Set branch protection rules on `main` — no direct pushes, PRs required
 
---
 
### Day-to-Day Development
- 🤖 Agent works on the `agent` branch only — never touches `main` directly
- 🤖 Agent creates branches, edits files, runs build tasks, opens PRs
- 🤖 Agent reads `CLAUDE.md` for context and conventions at session start
- 🤖 Agent self-diagnoses missing access and reports to Joel before proceeding
- 👤 Joel assigns issues to the agent or triggers it via `@claude` mention
- 👤 Joel reviews every PR the agent opens before anything merges
- 🔀 Agent opens PR → Joel reviews, requests changes if needed, then approves
- 👤 Joel merges approved PRs into `main` — agent never merges
- 👤 Joel resolves any merge conflicts on `main`
 
---
 
### Build Pipeline
- 🤖 Agent build: agent pushes to `agent` branch → `agent-build.yml` Action fires automatically
- 👤 User build: Joel pushes to `main` → `user-build.yml` Action fires as safety net
- 👤 Joel verifies minified output locally before committing in user build path
- 🔀 Agent-generated minified files are produced by GitHub Action — Joel reviews in PR before merge
 
---
 
### Deployments & Go-Live
- 👤 Joel is the final gate before anything goes to GitHub Pages / live site
- 👤 Joel approves the merge that triggers the live deployment
- 🤖 Agent may prepare the deployment-ready branch but cannot deploy independently
- [ ] **TODO**: Define and document the exact deployment trigger — is it the merge to `main` that deploys, or a separate step Joel kicks off manually?
 
---
 
### Social Posting Pipeline
- 🤖 Agent drafts the social preview image and caption
- 🔀 Agent presents the draft → Joel reviews and approves before posting
- 👤 Joel owns the Twitter/X and Discord accounts — agent posts on Joel's behalf only after approval
- 🤖 Agent logs the post to `CLAUDE.md` post log after Joel confirms it went out
- [ ] **TODO**: Decide if Joel wants a manual confirm step before every post, or a review window (e.g. 30 min to cancel) before auto-post
 
---
 
### Ongoing Maintenance
- [ ] As new workflows are added, tag every step as 👤 Joel, 🤖 Agent, or 🔀 Handoff
- [ ] Fold this matrix into `CLAUDE.md` so the agent always knows its lane
- [ ] Revisit and tighten permissions over time as trust in the agent workflow grows
 
---
 
## 💬 Agent Communication & Flagging System
 
> Research and build phase. Defines how the agent asks questions, sends alerts, and gets answers back.
> This section gets ported into both `humans.md` (Joel's reference) and `CLAUDE.md` (agent's instructions).
 
---
 
### Proposed Communication Flow
- 🤖 Agent hits a blocker, needs clarification, or has a high-token/complex task decision
- 🤖 Agent **bundles all pending questions** into a single comment on the relevant GitHub issue (saves tokens vs asking one at a time)
- 🤖 Agent sends a **Discord ping** to Joel via webhook: short message, links directly to the GitHub issue comment
- 👤 Joel sees the Discord ping, goes to the GitHub issue, replies in the thread
- 🤖 Agent reads Joel's reply from the GitHub issue comment on next run and continues
 
---
 
### Research Items
- [ ] **Discord webhook setup** — outbound only, no bot account needed for sending pings
  - Agent makes an HTTP POST to the webhook URL with the alert message
  - Webhook URL stored as a GitHub secret — never hardcoded
  - Message should include: issue number, brief summary of blocker, direct link to the GitHub comment
- [ ] **GitHub issue comment as Q&A thread** — agent posts bundled questions, Joel replies in thread
  - Agent reads the reply on next invocation via GitHub API (already available in the Action context)
  - No additional polling infrastructure needed
- [ ] **Decide on Discord target** — DM to Joel or a private channel in Joel's server
  - Private channel recommended: keeps a searchable log of all agent alerts
- [ ] **Bundling logic** — agent should collect all questions before pinging, not ping per question
  - Exception: if a single blocker is hard-stopping all progress, ping immediately
  - Otherwise batch and send together to minimize interruptions and token overhead
- [ ] **Flagging system for high-token or complex tasks**
  - If a task exceeds a token threshold or requires a judgment call, agent flags before proceeding
  - Agent describes what it *would* do, asks for confirm before taking action
  - Joel replies on GitHub issue: "proceed", "stop", or revised instructions
 
---
 
### Files to Create / Update
- [ ] **Create `humans.md`** — Joel's reference guide
  - Where the agent will ping you (Discord channel or DM)
  - What the ping will look like and what to do when you get one
  - Where to reply (GitHub issue thread on the relevant issue)
  - How to tell the agent to proceed, stop, or change course
  - How to reply to bundled questions (can answer inline, numbered, or in one block)
- [ ] **Update `CLAUDE.md`** — agent's communication instructions
  - When to send a Discord ping (blockers, bundled questions, high-token flags)
  - How to format the GitHub issue comment with questions (numbered list, clear and concise)
  - How to read Joel's replies from the issue thread
  - Never proceed past a hard blocker without a reply — report and wait
  - Bundle questions before pinging unless a single issue is fully blocking all progress
 
---
 
## 📏 Task Sizing & Complexity Rating
 
> T-shirt sizing system used by both Joel and the agent to scope, rate, and communicate task complexity before work begins.
> Agent should assess and declare its sizing estimate before starting any task, and flag if a task grows beyond its initial size mid-work.
 
---
 
### Size Definitions
 
**S — Small**
- Single language, single concern
- May touch multiple files but each change is straightforward and easy to trace
- Low risk of unintended side effects
- Examples: fix a CSS hover color, update a copy string, swap an icon, fix a broken link
 
**M — Medium**
- 2 or fewer languages involved
- 2–3+ files changed or affected
- Requires understanding how pieces connect but scope is still contained
- Examples: update a nav component that affects HTML structure and CSS, add a new footer icon with JS interaction, fix a bug that spans a template and a stylesheet
 
**L — Large**
- Near full-stack in scope — touches HTML, JavaScript, CSS, and/or a platform or third-party API
- High number of files affected or significant logic changes
- Higher risk of side effects across the codebase
- Examples: building the Tile Splash viewer with Sets, wiring up the social preview pipeline, setting up the GitHub Actions agent workflow
 
**XL — Extra Large** *(implicit escalation tier)*
- Anything that spans multiple L-sized concerns, requires architectural decisions, or touches deployment/infrastructure
- Agent should not begin XL tasks without explicit Joel approval and a written plan first
- Examples: full build pipeline setup, agent communication system end-to-end, new section of the site from scratch
 
---
 
### Agent Sizing Protocol
- [ ] Before starting any task, agent declares its size estimate and a one-line reason
  - Example: `"Sizing this as M — touching 2 files (HTML + CSS), single language concern"`
- [ ] If a task grows beyond its initial size mid-work, agent stops and flags Joel before continuing
  - Example: `"This started as S but I've found it affects 4 files across JS and CSS — re-sizing to M, flagging before I proceed"`
- [ ] XL tasks always require a written plan posted to the GitHub issue for Joel to approve before any code is touched
- [ ] Size estimates get logged alongside the task in the post log / change log over time — builds a calibration record
 
---
 
### Files to Update
- [ ] Add sizing definitions to `CLAUDE.md` so agent always has the rubric
- [ ] Add sizing legend to `humans.md` so Joel can read and challenge the agent's estimates
- [ ] Tag existing to-do items with a size estimate as part of the migration pass into humans/claude files
 
---
 
## 📁 File System & Agent Hierarchy Clarification
 
> Four files, four audiences. Everything in the to-do migrates into one or more of these.
 
---
 
### The Four Files
 
**`todo.md`** — The inbox
- Everything lands here first with full detail
- Gets migrated out into the other files during review sessions
- Not an agent instruction file — it's the staging area
 
**`humans.md`** — Joel's reference
- Process documentation written for a human
- Version and change log
- Where and how to respond to agent alerts
- Task sizing legend from Joel's perspective
- Constantly updated as processes evolve
 
**`agents.md`** — Universal agent boilerplate *(new file to create)*
- Broad project context any agent needs to get up and running
- Platform-agnostic — written for Claude, GPT, Gemini, local models, or anything else
- Covers: project overview, naming conventions, file structure, sizing system, communication expectations, branch conventions
- Goal: minimal setup friction, any agent reads this and can function on the project
- Does NOT include Claude-specific syntax or tool references
 
**`CLAUDE.md`** — Claude's operating manual
- Inherits everything from `agents.md` and goes deeper
- Claude-specific instructions, tool usage, MCP config, GitHub Action behavior
- Lead agent protocols: XL task questioning, bundled Q&A, Discord ping format, self-diagnostics
- Purpose-driven — Claude is the lead agent, this file reflects that responsibility
 
---
 
### XL Task Protocol Update
- [ ] XL tasks require the agent to run a **line of questioning pass first**
  - Before writing a plan, agent surfaces all unknowns, assumptions, and possible oversights as a bundled question set to Joel
  - Joel answers, agent then writes the plan incorporating those answers
  - Plan gets posted to the GitHub issue for Joel's approval before any code is touched
  - Only after plan approval does the agent begin work
  - This two-step (questions → plan → approval → work) prevents large efforts going sideways from unaddressed unknowns
 
---
 
### Migration To-Do
- [ ] Create `agents.md` as the universal boilerplate agent file
- [ ] Audit existing `CLAUDE.md` and fold in what we've built here
- [ ] Migrate all relevant to-do sections into their target files:
  - Sizing system → `agents.md` + `humans.md`
  - Communication/flagging protocol → `agents.md` + `CLAUDE.md` + `humans.md`
  - Joel vs Agent touch point matrix → `humans.md` + `CLAUDE.md`
  - Self-diagnostic block → `CLAUDE.md`
  - Agent setup steps → `humans.md`
  - XL task protocol → `agents.md` + `CLAUDE.md` + `humans.md`
- [ ] Add a header to `todo.md` explaining its role as the inbox and the four-file system
 

